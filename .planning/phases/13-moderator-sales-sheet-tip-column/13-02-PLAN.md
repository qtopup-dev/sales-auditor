---
phase: 13-moderator-sales-sheet-tip-column
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - packages/backend/src/routes/shifts.ts
  - packages/backend/src/routes/admin.ts
  - packages/frontend/src/components/shift/ShiftTotalsBanner.tsx
  - packages/frontend/src/pages/SalesPage.tsx
  - packages/frontend/src/pages/AdminShiftsPage.tsx
  - packages/frontend/src/components/admin/StatCard.tsx
  - packages/frontend/src/pages/DashboardPage.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-16, D-19, D-21]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Revenue This Shift (GET /api/shifts/current), moderator Shift History revenue (GET /api/shifts/history), each admin Shifts tab (GET /api/admin/shifts), dashboard Total Revenue (GET /api/admin/summary) and the Profit/Turnover KPI periods all equal SUM(priceSnapshot + tip) over active rows (D-01)"
    - "The dashboard per-product revenue breakdown stays SUM(priceSnapshot) only, with no tip (D-02)"
    - "Voided rows contribute neither price nor tip to any revenue figure. The existing status 'active' filters and CASE WHEN status = 'active' guards are unchanged (D-03)"
    - "The Revenue This Shift banner on the moderator Sales sheet and on every admin Shifts tab shows the combined total, plus a smaller `incl. ₱X tips` line fed by a separate `activeSalesTips` DECIMAL string. The line is hidden when tips are 0.00 (D-04)"
    - "The dashboard Total Revenue card shows an `incl. ₱X tips` caption from the summary endpoint's `totalTips` string (D-05)"
    - "The Profit/Turnover KPI cards and the Shift History Revenue column show the combined number only, with no tips caption (D-06)"
    - "Every revenue sum is computed by MySQL SUM or Prisma Decimal `.add()`, never JS float arithmetic. The admin Shifts tab float accumulator is replaced by Decimal accumulators (D-16, Rule 6)"
    - "The AdminShiftsPage per-shift sale list has a right-aligned Tip column after Price, empty when there is no tip (D-19, D-21)"
  artifacts:
    - path: "packages/backend/src/routes/shifts.ts"
      provides: "combined activeSalesRevenue + activeSalesTips on /current; combined revenue on /history"
      contains: "activeSalesTips"
    - path: "packages/backend/src/routes/admin.ts"
      provides: "totalTips + combined totalRevenue, tip-inclusive KPI SQL, Decimal-summed admin shift tabs with activeSalesTips, tip on admin shift sale rows"
      contains: "priceSnapshot + COALESCE(tip, 0)"
    - path: "packages/frontend/src/components/shift/ShiftTotalsBanner.tsx"
      provides: "optional `tips` prop rendering the `incl. ₱X tips` secondary line"
      contains: "tips?: string"
    - path: "packages/frontend/src/components/admin/StatCard.tsx"
      provides: "optional `caption` prop"
      contains: "caption?: string"
  key_links:
    - from: "packages/frontend/src/pages/SalesPage.tsx"
      to: "GET /api/shifts/current"
      via: "currentShift.activeSalesTips → ShiftTotalsBanner tips prop"
      pattern: "tips=\\{currentShift"
    - from: "packages/frontend/src/pages/AdminShiftsPage.tsx"
      to: "GET /api/admin/shifts"
      via: "selectedTab.activeSalesTips → ShiftTotalsBanner tips prop; sale.tip → Tip column"
      pattern: "tips=\\{selectedTab"
    - from: "packages/frontend/src/pages/DashboardPage.tsx"
      to: "GET /api/admin/summary"
      via: "summary.totalTips → StatCard caption"
      pattern: "totalTips"
---

<objective>
Make tips count in every revenue figure in the app, except the per-product breakdown. Show the tip share as an `incl. ₱X tips` line on the Revenue This Shift banner (moderator sheet + admin Shifts tabs) and on the dashboard Total Revenue card. Add the Tip column to the admin per-shift sale list.

Purpose: covers D-01..D-06 and D-19 on top of the `sales.tip` column and `Sale.tip` type from Plan 13-01. Task 1 changes the numbers users already see, so after it every revenue figure includes tips. Task 2 adds the captions and the admin shift Tip column.

Output: tip-aware aggregates in `shifts.ts` / `admin.ts`; `tips` prop on ShiftTotalsBanner; `caption` prop on StatCard; wired pages.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-moderator-sales-sheet-tip-column/13-CONTEXT.md
@.planning/phases/13-moderator-sales-sheet-tip-column/13-PATTERNS.md
@.planning/phases/13-moderator-sales-sheet-tip-column/13-01-SUMMARY.md

<interfaces>
<!-- Produced by Plan 13-01 (already in place): -->
Prisma Sale.tip: Prisma.Decimal | null   (aggregate/groupBy accept `_sum: { tip: true }`)
Shared Sale.tip: string | null
Prisma namespace import for Decimal math: import { Prisma } from '../generated/prisma/client.js';  → new Prisma.Decimal(x).add(y).toFixed(2)

<!-- Produced by this plan: -->
GET /api/shifts/current  → { ...shift, activeSalesCount, activeSalesRevenue: string (price+tip), activeSalesTips: string }
GET /api/shifts/history  → [{ ...shift, activeSalesCount, activeSalesRevenue: string (price+tip) }]      // no tips field (D-06)
GET /api/admin/summary   → { totalCount, totalRevenue: string (price+tip), totalTips: string, ... productBreakdown revenue price-only }
GET /api/admin/shifts    → tabs[]: { ..., activeSalesRevenue: string (price+tip), activeSalesTips: string, sales[]: { ..., tip: string | null } }
ShiftTotalsBanner props: { count: number; revenue: string; tips?: string; loading?: boolean }
StatCard props: { label: string; value: string; caption?: string; loading?: boolean }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Every revenue figure includes tips (shifts + admin endpoints), Decimal-safe, with separate tips sums for the captions</name>
  <reversibility rating="costly">D-01 (rated costly in CONTEXT.md): changes every revenue aggregate and all 8 KPI raw-SQL sums. Reverting means touching every site again. No data migration is involved.</reversibility>
  <files>packages/backend/src/routes/shifts.ts, packages/backend/src/routes/admin.ts</files>
  <read_first>
    - packages/backend/src/routes/shifts.ts (whole file: /current aggregate L116-126, /history groupBy L149-165)
    - packages/backend/src/routes/admin.ts (whole file: toMoneyStr L16-22, summary aggregate L52-55, productBreakdown groupBy L58-64, 8 KPI raw SQL sums L130-167, totalRevenue L173, serializeSaleForAdminShifts L230-254, perShiftAgg L311-317, tab accumulation L326-343)
    - packages/backend/src/lib/prisma.ts (import path convention for the generated client)
    - .planning/phases/13-moderator-sales-sheet-tip-column/13-CONTEXT.md (D-01..D-06, D-16)
  </read_first>
  <action>
1. `shifts.ts`. Add `import { Prisma } from '../generated/prisma/client.js';`.
   - GET /current (D-01, D-04, D-16): change the aggregate to `_sum: { priceSnapshot: true, tip: true }`. Return `activeSalesRevenue: new Prisma.Decimal(agg._sum.priceSnapshot ?? 0).add(agg._sum.tip ?? 0).toFixed(2)` and add `activeSalesTips: (agg._sum.tip ?? 0).toFixed(2)`.
   - GET /history (D-01, D-06): add `tip: true` to the groupBy `_sum`. Compute `activeSalesRevenue` as the Decimal sum of price and tip, the same way as /current. Add no tips field here.
   - Leave every `status: 'active'` filter exactly as it is (D-03). Update the Rule 6 comment above /current to mention price + tip.

2. `admin.ts`. Add the same `Prisma` import.
   - GET /summary (D-01, D-05):
     - The `revenueResult` aggregate becomes `_sum: { priceSnapshot: true, tip: true }`.
     - `totalRevenue` becomes `new Prisma.Decimal(revenueResult._sum.priceSnapshot ?? 0).add(revenueResult._sum.tip ?? 0).toFixed(2)`.
     - Add `totalTips: (revenueResult._sum.tip ?? 0).toFixed(2)` next to it.
     - Update the header comment's field list (`totalRevenue` = price + tip, plus the new `totalTips`).
   - Product breakdown (D-02): do NOT touch the `productBreakdown` groupBy or its `revenue` mapping. It stays price-only.
   - KPI raw SQL (D-01, D-06): in all 8 `profitSum` / `turnoverSum` expressions (4 period queries × 2 columns), change the operand after `THEN` from the bare price column to `priceSnapshot + COALESCE(tip, 0)`. The `CASE WHEN status = 'active'` guard, the `ELSE 0`, the WHERE clauses and `toMoneyStr` stay as they are. MySQL DECIMAL + DECIMAL stays DECIMAL, which `toMoneyStr` already handles.
   - GET /shifts (D-01, D-04, D-16, D-19):
     - Add `tip: { toFixed: (n: number) => string } | null;` to the `serializeSaleForAdminShifts` input type and `tip: sale.tip?.toFixed(2) ?? null,` to its output, right after `priceSnapshot`.
     - Add `tip: true` to the `perShiftAgg` groupBy `_sum`.
     - In the tab loop, remove the float accumulator variable (`activeRevenueRaw`) and its `Number(...)` addition. Replace them with two Decimal accumulators, `let activeRevenue = new Prisma.Decimal(0)` and `let activeTips = new Prisma.Decimal(0)`. For each matching agg, add `priceSnapshot ?? 0` and `tip ?? 0` to `activeRevenue`, and `tip ?? 0` to `activeTips`, using `.add()`.
     - Return `activeSalesRevenue: activeRevenue.toFixed(2)` and `activeSalesTips: activeTips.toFixed(2)`.
     - Update the loop comment to say the sums are Decimal (no JS float). Leave `toMoneyStr` in place, since the KPI mapping still uses it.
  </action>
<!-- planner-discipline-allow: tip -->
  <verify>
    <automated>cd "D:/project/custom projects/alejinput" && npx tsc -p packages/backend/tsconfig.json --noEmit && test "$(grep -c 'priceSnapshot + COALESCE(tip, 0)' packages/backend/src/routes/admin.ts)" = 8 && test "$(grep -c 'THEN priceSnapshot ELSE' packages/backend/src/routes/admin.ts)" = 0 && test "$(grep -A4 "by: \['productNameSnapshot'\]" packages/backend/src/routes/admin.ts | grep -c 'tip')" = 0 && test "$(grep -c 'Number(agg' packages/backend/src/routes/admin.ts)" = 0 && echo "STATIC OK"</automated>
    <automated>cd "D:/project/custom projects/alejinput" && B=http://localhost:3001/api && J=.tip-rev-jar && H='Content-Type: application/json' &&
cents() { grep -o "\"$1\":\"[0-9.]*\"" | grep -o '[0-9]*\.[0-9]*' | tr -d .; } &&
curl -sf -c $J -H "$H" -d '{"username":"admin","password":"admin1234"}' $B/auth/login >/dev/null &&
PR=$(curl -s -b $J $B/catalog/products) && P=$(echo "$PR" | grep -o '"id":[0-9]*' | head -1 | cut -d: -f2) && PC=$(echo "$PR" | grep -o '"price":"[0-9.]*"' | head -1 | grep -o '[0-9]*\.[0-9]*' | tr -d .) &&
M=$(curl -s -b $J $B/catalog/mops | grep -o '"id":[0-9]*' | head -1 | cut -d: -f2) &&
R=$(curl -s -b $J $B/catalog/receivers | grep -o '"id":[0-9]*' | head -1 | cut -d: -f2) &&
S0=$(curl -s -b $J $B/admin/summary) && T0=$(echo "$S0" | cents totalTips) && V0=$(echo "$S0" | cents totalRevenue) &&
ID=$(curl -s -b $J -H "$H" -d "{\"productId\":$P,\"mopId\":$M,\"receiverId\":$R,\"tip\":\"15\"}" $B/sales | grep -o '"id":[0-9]*' | head -1 | cut -d: -f2) &&
S1=$(curl -s -b $J $B/admin/summary) && T1=$(echo "$S1" | cents totalTips) && V1=$(echo "$S1" | cents totalRevenue) &&
test $((10#$T1 - 10#$T0)) -eq 1500 && test $((10#$V1 - 10#$V0)) -eq $((10#$PC + 1500)) &&
curl -s -b $J -X POST -H "$H" -d '{}' $B/sales/$ID/void | grep -q '"status":"void"' &&
S2=$(curl -s -b $J $B/admin/summary) && test "$(echo "$S2" | cents totalTips)" = "$T0" && test "$(echo "$S2" | cents totalRevenue)" = "$V0" &&
test "$(curl -s -o /dev/null -w '%{http_code}' -b $J "$B/admin/shifts?date=$(TZ=Asia/Manila date +%F)")" = 200 &&
rm -f $J && echo "REVENUE OK"</automated>
  </verify>
  <acceptance_criteria>
    - Backend `tsc` exits 0
    - `grep -c 'priceSnapshot + COALESCE(tip, 0)' packages/backend/src/routes/admin.ts` returns 8, and no KPI sum still uses the bare price operand (static gate above prints `STATIC OK`)
    - The productBreakdown groupBy block contains no `tip` (D-02; static gate)
    - The admin Shifts tab accumulation no longer uses `Number(` on aggregate sums (static gate)
    - `grep -c "activeSalesTips" packages/backend/src/routes/shifts.ts` returns 1 and `grep -c "activeSalesTips" packages/backend/src/routes/admin.ts` returns 1
    - `grep -c "totalTips" packages/backend/src/routes/admin.ts` returns ≥ 1
    - `grep -c "tip: true" packages/backend/src/routes/shifts.ts` returns 2
    - The revenue e2e prints `REVENUE OK`. It shows three things: adding an active sale with tip 15 raises `totalTips` by exactly 15.00 and `totalRevenue` by exactly price + 15.00 (D-01, D-05); voiding it returns both to their starting values (D-03); and /admin/shifts still answers 200
  </acceptance_criteria>
  <done>Every revenue endpoint returns price + tip over active rows using Decimal/SQL math. `/current`, `/admin/shifts` tabs and `/admin/summary` also return the tips-only sum. The product breakdown is untouched. The e2e revenue delta check passes. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: `incl. ₱X tips` captions on the shift banner and dashboard Total Revenue; Tip column on the admin per-shift sale list</name>
  <files>packages/frontend/src/components/shift/ShiftTotalsBanner.tsx, packages/frontend/src/pages/SalesPage.tsx, packages/frontend/src/pages/AdminShiftsPage.tsx, packages/frontend/src/components/admin/StatCard.tsx, packages/frontend/src/pages/DashboardPage.tsx</files>
  <read_first>
    - packages/frontend/src/components/shift/ShiftTotalsBanner.tsx (whole file: props L6-10, addThousandsSep L13-17, Revenue card L30-38)
    - packages/frontend/src/pages/SalesPage.tsx (CurrentShiftWithTotals L14-20, banner usage L100-105)
    - packages/frontend/src/pages/AdminShiftsPage.tsx (AdminShiftSaleRow L11-20, AdminShiftTab L22-30, banner L104, table header L130-138, price cell L153)
    - packages/frontend/src/components/admin/StatCard.tsx
    - packages/frontend/src/pages/DashboardPage.tsx (AdminSummary L34-45, revenueValue L106-109, Total Revenue StatCard L157-161)
    - .planning/phases/13-moderator-sales-sheet-tip-column/13-CONTEXT.md (D-04, D-05, D-06, D-19, D-21, specifics: `incl. ₱350.00 tips`)
  </read_first>
  <action>
1. `ShiftTotalsBanner.tsx` (D-04):
   - Add the optional prop `tips?: string`, commented as a DECIMAL string of the tips share (Rule 6).
   - In the Revenue card's non-loading branch, keep the existing figure. Under it, render `<p className="text-xs font-normal text-gray-500 dark:text-gray-400 mt-1">` with the text `incl. ₱{addThousandsSep(tips)} tips`, only when `tips` is set and is not `'0.00'` (hidden at zero, Claude's discretion).
   - Use pure string formatting via the existing `addThousandsSep`, with no conversion to a JS number.
   - The Revenue This Shift figure itself stays `revenue`, which is already combined by the backend.

2. `SalesPage.tsx` (D-04): add `activeSalesTips: string` to `CurrentShiftWithTotals` and pass `tips={currentShift!.activeSalesTips}` to `ShiftTotalsBanner`.

3. `AdminShiftsPage.tsx` (D-04, D-19, D-21):
   - Add `tip: string | null` to `AdminShiftSaleRow` and `activeSalesTips: string` to `AdminShiftTab`.
   - Pass `tips={selectedTab.activeSalesTips}` to `ShiftTotalsBanner`.
   - In the per-shift table, insert a header cell reading `Tip` right after the Price header, with the Price header's classes (`text-right`).
   - Insert a body cell right after the price cell, with the price cell's classes, rendering `{sale.tip ?? ''}` (empty when no tip).

4. `StatCard.tsx` (D-05): add the optional prop `caption?: string`. In the non-loading branch, under the value, render `<p className="text-xs font-normal text-gray-500 dark:text-gray-400 mt-1">{caption}</p>` only when `caption` is set.

5. `DashboardPage.tsx` (D-05, D-06):
   - Add `totalTips: string` to `AdminSummary`.
   - Next to `revenueValue`, derive `tipsCaption`: when `summary` exists and `summary.totalTips !== '0.00'`, it is `'incl. ₱' + summary.totalTips.replace(/\B(?=(\d{3})+(?!\d))/g, ',') + ' tips'`, using the same string-regex separator already used for `revenueValue` (no float). Otherwise it is `undefined`.
   - Pass `caption={tipsCaption}` to the Total Revenue `StatCard` only.
   - Leave the KPI cards (Profit/Turnover) and the Shift History page unchanged: they show combined numbers only (D-06), already supplied by the backend.
  </action>
  <verify>
    <automated>cd "D:/project/custom projects/alejinput" && npx tsc -p packages/frontend/tsconfig.json && grep -q "tips?: string" packages/frontend/src/components/shift/ShiftTotalsBanner.tsx && grep -q "incl\. " packages/frontend/src/components/shift/ShiftTotalsBanner.tsx && grep -q "tips={currentShift!.activeSalesTips}" packages/frontend/src/pages/SalesPage.tsx && grep -q "tips={selectedTab.activeSalesTips}" packages/frontend/src/pages/AdminShiftsPage.tsx && grep -q ">Tip</th>" packages/frontend/src/pages/AdminShiftsPage.tsx && grep -q "sale.tip ?? ''" packages/frontend/src/pages/AdminShiftsPage.tsx && grep -q "caption?: string" packages/frontend/src/components/admin/StatCard.tsx && grep -q "caption={tipsCaption}" packages/frontend/src/pages/DashboardPage.tsx && echo "UI OK"</automated>
  </verify>
  <acceptance_criteria>
    - Frontend `tsc` exits 0 and the verify chain prints `UI OK`
    - `grep -c "parseFloat\|Number(" packages/frontend/src/components/shift/ShiftTotalsBanner.tsx` returns 0 (Rule 6: string-only money formatting)
    - In `AdminShiftsPage.tsx`, the line numbers from `grep -n ">Price</th>\|>Tip</th>\|>MOP</th>"` are strictly increasing in the order Price, Tip, MOP (D-19)
    - `grep -c "caption=" packages/frontend/src/pages/DashboardPage.tsx` returns 1 (only the Total Revenue card gets a caption; KPI cards unchanged, D-06)
  </acceptance_criteria>
  <done>Both banners and the dashboard Total Revenue card show `incl. ₱X tips` when tips are above zero. The admin per-shift list has an empty-when-null Tip column after Price. The frontend type-checks. Committed.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

| Artifact | Kind | Location |
|---|---|---|
| `activeSalesTips: string` on GET /api/shifts/current | API field | packages/backend/src/routes/shifts.ts |
| `activeSalesTips: string` on each GET /api/admin/shifts tab | API field | packages/backend/src/routes/admin.ts |
| `totalTips: string` on GET /api/admin/summary | API field | packages/backend/src/routes/admin.ts |
| `tip: string \| null` in `serializeSaleForAdminShifts` output | API field | packages/backend/src/routes/admin.ts |
| KPI SQL operand `priceSnapshot + COALESCE(tip, 0)` (×8) | SQL change | packages/backend/src/routes/admin.ts |
| Decimal accumulators `activeRevenue`, `activeTips` | local vars | packages/backend/src/routes/admin.ts |
| `ShiftTotalsBanner` prop `tips?: string` | component prop | packages/frontend/src/components/shift/ShiftTotalsBanner.tsx |
| `StatCard` prop `caption?: string` | component prop | packages/frontend/src/components/admin/StatCard.tsx |
| `CurrentShiftWithTotals.activeSalesTips` | local interface field | packages/frontend/src/pages/SalesPage.tsx |
| `AdminShiftTab.activeSalesTips`, `AdminShiftSaleRow.tip`, Tip `<th>`/`<td>` | interface fields + table column | packages/frontend/src/pages/AdminShiftsPage.tsx |
| `AdminSummary.totalTips`, `tipsCaption` | interface field + derived value | packages/frontend/src/pages/DashboardPage.tsx |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → browser (aggregate endpoints) | Server-computed money strings. There is no user input on these read paths beyond the regex-validated `date` query |
| API → MySQL ($queryRaw) | Tagged-template raw SQL; `organizationId` comes from the session and is parameterized |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Tampering (integrity) | revenue totals drifting via JS float | medium | mitigate | All sums use MySQL SUM, or Prisma.Decimal `.add()` followed by `.toFixed(2)`. The admin tab float accumulator is removed (D-16, Rule 6) |
| T-13-07 | Information Disclosure | voided rows' tips leaking into totals | medium | mitigate | The existing `status: 'active'` filters and the `CASE WHEN status = 'active'` guards are kept verbatim. The e2e void round-trip proves totals return to baseline (D-03) |
| T-13-08 | Elevation of Privilege | new tips fields exposed across roles | low | accept | Fields ride on existing routes with unchanged guards: `shiftsRouter` requireRole('moderator') scoped to the caller's own shift, and `adminRouter` requireRole('admin') |
| T-13-09 | Tampering (SQL injection) | edited KPI raw SQL | low | mitigate | Only the constant column expression changes. The queries stay Prisma tagged templates with parameterized `organizationId` |
</threat_model>

<verification>
- Backend and frontend `tsc` pass
- The static gate prints `STATIC OK`: 8 tip-inclusive KPI sums, product breakdown untouched, no float accumulator
- The revenue e2e prints `REVENUE OK`: tip 15 adds exactly 15.00 to `totalTips` and price + 15.00 to `totalRevenue`, and voiding restores both
- The UI grep chain prints `UI OK`
</verification>

<success_criteria>
Moderators see Revenue This Shift including tips, with an `incl. ₱X tips` line. Admins see the same on every Shifts tab and on the dashboard Total Revenue card. KPIs and Shift History include tips silently, and the per-product chart stays price-only.
</success_criteria>

<output>
Create `.planning/phases/13-moderator-sales-sheet-tip-column/13-02-SUMMARY.md` when done
</output>
