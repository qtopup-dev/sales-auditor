---
phase: 13-moderator-sales-sheet-tip-column
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - packages/frontend/src/components/admin/AdminSalesTable.tsx
  - packages/frontend/src/components/admin/VoidRequestsTable.tsx
autonomous: true
requirements: [D-17, D-18, D-20, D-21]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The dashboard AdminSalesTable shows a right-aligned Tip column right after Price. Admins can click it to edit the tip inline through the same EditableCell/PATCH flow as the Sales sheet, and voided rows are not editable (D-17)"
    - "The dashboard CSV export has a Tip column right after Price, passed through sanitizeCell, with an empty value when there is no tip (D-18, D-21)"
    - "The admin Void Requests table shows a right-aligned, display-only Tip column right after Price (D-20)"
    - "Rows with no tip render an empty Tip cell in both tables, with no dash (D-21)"
  artifacts:
    - path: "packages/frontend/src/components/admin/AdminSalesTable.tsx"
      provides: "editable Tip column + CSV Tip field"
      contains: "label: 'Tip'"
    - path: "packages/frontend/src/components/admin/VoidRequestsTable.tsx"
      provides: "display-only Tip column"
      contains: "sale.tip"
  key_links:
    - from: "packages/frontend/src/components/admin/AdminSalesTable.tsx"
      to: "packages/frontend/src/components/sales/EditableCell.tsx"
      via: "<EditableCell sale={row.original} field=\"tip\" ...> (reuses PATCH /api/sales/:id, admin passes canEdit-or-admin gate)"
      pattern: "field=\"tip\""
    - from: "packages/frontend/src/components/admin/AdminSalesTable.tsx"
      to: "CSV download"
      via: "sanitizeCell(row.tip ?? '') + fields entry { label: 'Tip', value: 'tip' }"
      pattern: "sanitizeCell\\(row\\.tip"
---

<objective>
Surface the tip in the admin tables. The dashboard sales table gets an inline-editable Tip column plus a Tip column in its CSV export, and the Void Requests table gets a display-only Tip column.

Purpose: covers D-17, D-18 and D-20 (and D-21 for these surfaces). It depends only on Plan 13-01, which provides `Sale.tip`, the `tip` field on `EditableCell`, and the serialized `tip` that `voidRequests.ts` already passes through via `serializeSale`. It shares no files with Plan 13-02, so both run in parallel in wave 2.

Output: two edited admin table components.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-moderator-sales-sheet-tip-column/13-CONTEXT.md
@.planning/phases/13-moderator-sales-sheet-tip-column/13-01-SUMMARY.md

<interfaces>
<!-- Produced by Plan 13-01 (already in place): -->
Shared Sale.tip: string | null        // also present on VoidRequestWithSale.sale
EditableCell props: { sale: Sale; field: 'productId' | 'mopId' | 'receiverId' | 'notes' | 'tip'; displayValue: string }
  // tip mode: right-aligned, digits/'.' only, inline error, empty when no tip, invalidates ['sales'] + ['admin-summary'] on save
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard sales table — inline-editable Tip column after Price, and Tip in the CSV export</name>
  <files>packages/frontend/src/components/admin/AdminSalesTable.tsx</files>
  <read_first>
    - packages/frontend/src/components/admin/AdminSalesTable.tsx (whole file: header comment L1-4, sanitizeCell L23-30, downloadCSV rows L38-49 and fields L51-62, priceSnapshot column L110-118)
    - packages/frontend/src/components/sales/SalesTable.tsx (the Tip column Plan 13-01 added: the exact column shape to mirror)
    - packages/frontend/src/components/sales/EditableCell.tsx (tip mode from Plan 13-01)
    - .planning/phases/13-moderator-sales-sheet-tip-column/13-CONTEXT.md (D-17, D-18, D-21)
  </read_first>
  <action>
1. Add an import for `EditableCell` from `'../sales/EditableCell'`. In the header comment, note that the table is read-only except for the inline-editable Tip column (D-17).

2. In `columns`, insert a new column object immediately after the `priceSnapshot` column (D-17):
   - `accessorKey: 'tip'`
   - `header: () => <span className="block text-right">Tip</span>`
   - `size: 100`
   - `cell: ({ row }) => <EditableCell sale={row.original} field="tip" displayValue={row.original.tip ?? ''} />`

   EditableCell already enforces the existing admin permission (admins are always editable, voided rows never are) plus the tip input rules. Add no new permission logic. The `useMemo` dependency array is unchanged because EditableCell is a module import.

3. CSV (D-18, D-21):
   - In `downloadCSV`'s `sanitizedRows` mapping, add `tip: sanitizeCell(row.tip ?? ''),` directly after `priceSnapshot`. Null becomes an empty value, and the value still goes through formula-injection sanitization.
   - In `fields`, add `{ label: 'Tip', value: 'tip' },` directly after the Price entry, aligned like its neighbours.
  </action>
  <verify>
    <automated>cd "D:/project/custom projects/alejinput" && npx tsc -p packages/frontend/tsconfig.json && grep -q 'field="tip"' packages/frontend/src/components/admin/AdminSalesTable.tsx && grep -q "sanitizeCell(row.tip ?? '')" packages/frontend/src/components/admin/AdminSalesTable.tsx && test "$(grep -o "label: '[^']*'" packages/frontend/src/components/admin/AdminSalesTable.tsx | sed -n 3p)" = "label: 'Tip'" && test "$(grep -o "accessorKey: '[a-zA-Z]*'" packages/frontend/src/components/admin/AdminSalesTable.tsx | sed -n 3p)" = "accessorKey: 'tip'" && echo "ADMIN TABLE OK"</automated>
  </verify>
  <acceptance_criteria>
    - Frontend `tsc` exits 0 and the verify chain prints `ADMIN TABLE OK`
    - The third `accessorKey` in AdminSalesTable is `'tip'` (Product, Price, Tip order, D-17)
    - The third CSV field label is `'Tip'` (Product, Price, Tip, D-18)
    - `grep -c "sanitizeCell(row.tip ?? '')" packages/frontend/src/components/admin/AdminSalesTable.tsx` returns 1
  </acceptance_criteria>
  <done>The dashboard table shows an editable, right-aligned Tip column after Price that is empty when there is no tip. The CSV export has a sanitized Tip column after Price. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Void Requests table — display-only Tip column after Price</name>
  <files>packages/frontend/src/components/admin/VoidRequestsTable.tsx</files>
  <read_first>
    - packages/frontend/src/components/admin/VoidRequestsTable.tsx (header comment L1-4, `price` column L82-90)
    - .planning/phases/13-moderator-sales-sheet-tip-column/13-CONTEXT.md (D-20, D-21)
  </read_first>
  <action>
1. In `columns`, insert a column object immediately after the `price` column (D-20):
   - `id: 'tip'`
   - `header: () => <span className="block text-right">Tip</span>`
   - `size: 100`
   - `cell: ({ row }) => <span className="block text-right text-sm text-gray-900 dark:text-gray-100">{row.original.sale.tip ?? ''}</span>`

   It is display-only (void-request rows are not edited) and empty when there is no tip (D-21). Keep the price column's "never parseFloat" convention: render the string as-is.

2. Update the header comment's column list to include Tip after Price. It becomes eight sale columns: Product/Price/Tip/MOP/Receiver/Notes/Created By/Created At.

The backend needs no change: `voidRequests.ts` serializes `vr.sale` through `serializeSale`, which returns `tip` since Plan 13-01.
  </action>
  <verify>
    <automated>cd "D:/project/custom projects/alejinput" && npx tsc -p packages/frontend/tsconfig.json && grep -q "row.original.sale.tip ?? ''" packages/frontend/src/components/admin/VoidRequestsTable.tsx && test "$(grep -o "id: '[a-z]*'" packages/frontend/src/components/admin/VoidRequestsTable.tsx | sed -n 3p)" = "id: 'tip'" && echo "VOID TABLE OK"</automated>
  </verify>
  <acceptance_criteria>
    - Frontend `tsc` exits 0 and the verify chain prints `VOID TABLE OK`
    - The third column `id` in VoidRequestsTable is `'tip'` (Product, Price, Tip order, D-20)
    - `grep -c "row.original.sale.tip ?? ''" packages/frontend/src/components/admin/VoidRequestsTable.tsx` returns 1
  </acceptance_criteria>
  <done>The Void Requests table shows the row's tip next to Price, empty when there is no tip. Committed.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

| Artifact | Kind | Location |
|---|---|---|
| Tip column (`accessorKey: 'tip'`, `<EditableCell field="tip">`) | editable table column | packages/frontend/src/components/admin/AdminSalesTable.tsx |
| CSV row key `tip: sanitizeCell(row.tip ?? '')` + field `{ label: 'Tip', value: 'tip' }` | CSV column | packages/frontend/src/components/admin/AdminSalesTable.tsx |
| Tip column (`id: 'tip'`) | display-only table column | packages/frontend/src/components/admin/VoidRequestsTable.tsx |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → exported CSV opened in Excel | Cell values can be interpreted as formulas |
| admin browser → PATCH /api/sales/:id (tip) | Same untrusted-input boundary as Plan 13-01, enforced server-side there |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10 | Tampering (CSV formula injection) | Tip CSV column | medium | mitigate | `tip` passes through the existing `sanitizeCell` like every other text field (D-18). Server-side validation also makes a leading `=`/`-`/`+`/`@` impossible |
| T-13-11 | Elevation of Privilege | admin inline tip edit on the dashboard | low | mitigate | Reuses EditableCell and PATCH /api/sales/:id. The backend canEdit-or-admin check, the status 'active' check and parseTip validation (Plan 13-01) are the enforcement; the UI is convenience only (Rule 9) |
</threat_model>

<verification>
- Frontend `tsc` passes
- `ADMIN TABLE OK` and `VOID TABLE OK` print
- Manual (UAT, /gsd-verify-work): on the dashboard, an admin edits a tip inline, the CSV opens with Tip after Price, and a void request shows the tip
</verification>

<success_criteria>
Admins see and edit tips on the dashboard sales table, export them in CSV, and see them on Void Requests. Rows without a tip stay visually empty.
</success_criteria>

<output>
Create `.planning/phases/13-moderator-sales-sheet-tip-column/13-03-SUMMARY.md` when done
</output>
