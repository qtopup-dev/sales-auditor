---
phase: 999-01-fix-add-row-catalog
plan: "01"
status: complete
completed: "2026-06-24"
---

## Summary

Created `packages/backend/src/routes/catalog.ts` with two GET endpoints (`/products` and `/mops`) accessible to all authenticated users (no `requireRole` call). Mounted the router at `/catalog` on the existing `protectedRouter` in `app.ts` so all requests require a valid session via `requireAuth` but no admin role.

## What Was Built

- **New file:** `packages/backend/src/routes/catalog.ts` — exports `catalogRouter` with:
  - `GET /api/catalog/products` — returns `[{ id, name, price }]` with `price.toFixed(2)`; active-only via `$extends softDeleteFilter`
  - `GET /api/catalog/mops` — returns `[{ id, name }]`; active-only via `$extends softDeleteFilter`
- **Modified:** `packages/backend/src/app.ts` — added `catalogRouter` import and `protectedRouter.use('/catalog', catalogRouter)` mount after `salesRouter`

## Key Decisions / Deviations

No deviations from plan. Followed all directives exactly:
- No `requireRole` on catalog routes (D-04/D-05)
- No `isActive: undefined` override — `$extends` applies `isActive: true` by default (D-06)
- `.toFixed(2)` for price serialization
- Import uses `.js` extension for Node16 moduleResolution

## Acceptance Criteria

- [x] `packages/backend/src/routes/catalog.ts` exists
- [x] Exports `catalogRouter` with two GET routes
- [x] No `requireRole` call in catalog.ts
- [x] `.toFixed(2)` for price; no `isActive: undefined`
- [x] `app.ts` imports and mounts `catalogRouter` at `/catalog` on `protectedRouter`
- [x] Existing routes (usersRouter, productsRouter, mopsRouter, salesRouter) unchanged
- [x] No errors from catalog.ts in TypeScript check

## Self-Check: PASSED
