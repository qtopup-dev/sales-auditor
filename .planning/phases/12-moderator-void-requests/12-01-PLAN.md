---
phase: 12-moderator-void-requests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/prisma/schema.prisma
  - packages/backend/prisma/migrations/<generated>/migration.sql
  - packages/shared/src/types/voidRequest.ts
  - packages/shared/src/types/index.ts
autonomous: true
requirements: [PHASE12-SC1, PHASE12-SC2, PHASE12-SC4]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `void_requests` table exists in the live MySQL database with columns id, organizationId, saleId, reason, status, requestedById, requestedByUsername, reviewedById, reviewedByUsername, reviewedAt, createdAt, updatedAt"
    - "The `void_requests.status` column is a MySQL ENUM of exactly ('pending','approved','rejected') defaulting to 'pending' — a new request is born pending and never voids anything by itself"
    - "A DB-level unique index on (organizationId, pendingLock) — where `pendingLock` is a STORED generated column equal to `saleId` only while status='pending' and NULL otherwise — makes 'at most one pending Void Request per sale row' (D-02) race-safe: two truly simultaneous inserts cannot both persist a pending request, the loser hits Prisma P2002 (mirrors the Phase 7 `shifts.openLock` precedent, security_context T-07-01)"
    - "Because `pendingLock` collapses to NULL once a request leaves 'pending', a rejected request does not occupy the unique slot and the same sale row can receive a new pending request afterwards (D-03 — no permanent lockout at the DB layer)"
    - "The `reason` field has no special encoding contract beyond standard UTF-8 text storage: it is MySQL `TEXT` under utf8mb4 exactly like the existing `sales.notes` column, with no length normalization, grapheme counting, or byte-length validation applied"
    - "`void_requests` carries `organizationId` with a FK to organizations, per CLAUDE.md Rule 5 (organization_id on every business table)"
    - "The migration adds only the new `void_requests` table — no column, index, or constraint on sales, audit_log, shifts, products, mops, users, receivers, or organizations is altered"
    - "`@alejinput/shared` exports `VoidRequestStatus`, `VoidRequest`, and `VoidRequestWithSale`, and both backend and frontend resolve them through the existing `packages/shared/src/types/index.ts` barrel"
    - "`VoidRequest.reviewedAt` / `createdAt` / `updatedAt` are typed as ISO 8601 UTC strings in the shared type, matching the Sale/Shift convention (CLAUDE.md Rule 7)"
  prohibitions:
    - statement: "A Void Request must never void, alter, or soft-delete its target sale row at creation time — only an explicit admin Approve may change `sales.status`"
      status: flagged-unverified
      verification: unverified
    - statement: "A rejected Void Request must never permanently block future Void Requests on the same sale row (D-03)"
      status: flagged-unverified
      verification: unverified
    - statement: "Void Request records must never be hard-deleted or purged after review — they are a permanent review record (CLAUDE.md Rule 3, soft-delete only)"
      status: flagged-unverified
      verification: unverified
  artifacts:
    - path: "packages/backend/prisma/schema.prisma"
      provides: "VoidRequestStatus enum + VoidRequest model + back-relations on Organization/User/Sale"
      contains: "model VoidRequest"
    - path: "packages/backend/prisma/migrations"
      provides: "Applied migration creating the void_requests table, its indexes, its pendingLock uniqueness guard, and its four FK constraints"
      contains: "CREATE TABLE `void_requests`"
    - path: "packages/shared/src/types/voidRequest.ts"
      provides: "VoidRequestStatus / VoidRequest / VoidRequestWithSale API response shapes"
      exports: ["VoidRequestStatus", "VoidRequest", "VoidRequestWithSale"]
    - path: "packages/shared/src/types/index.ts"
      provides: "Barrel re-export of the new void-request types"
      contains: "from './voidRequest.js'"
  key_links:
    - from: "packages/backend/prisma/schema.prisma"
      to: "packages/backend/prisma/migrations/<generated>/migration.sql"
      via: "model VoidRequest ↔ CREATE TABLE void_requests"
      pattern: "void_requests"
    - from: "packages/shared/src/types/index.ts"
      to: "packages/shared/src/types/voidRequest.ts"
      via: "export type { VoidRequestStatus, VoidRequest, VoidRequestWithSale }"
      pattern: "from '\\./voidRequest\\.js'"
---

<objective>
Create the data layer and shared type contract for the Phase 12 Void Request workflow: a dedicated `VoidRequest` Prisma model (Claude's-discretion decision from CONTEXT.md — a dedicated model, not a reuse of `AuditLog`, because the pending/approved/rejected lifecycle and the free-text Reason field do not fit `AuditLog`'s append-only field-delta shape), the live-database migration that creates it, and the `@alejinput/shared` types every later plan imports.

Purpose: Plans 12-02 (backend router), 12-03 (moderator UI) and 12-04 (admin UI) all depend on this table existing in the live database and on the shared types compiling. This plan defines the contract; it adds no endpoint and no UI. Per the schema-push gate, `npm run build` and `tsc` will pass on the later plans even if the migration is skipped (types come from the generated Prisma client, not the live DB), so Task 2 is `[BLOCKING]` — it must actually run.

Output: `void_requests` table + `pendingLock` uniqueness guard in the live MySQL database; regenerated Prisma client exposing `prisma.voidRequest`; `VoidRequestStatus` / `VoidRequest` / `VoidRequestWithSale` exported from `@alejinput/shared`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-moderator-void-requests/12-CONTEXT.md
@.planning/phases/12-moderator-void-requests/12-PATTERNS.md

<interfaces>
<!-- Target-state identifiers this plan must produce. Read the actual files listed in each
     task's <read_first> for current contents and surrounding conventions. -->

Prisma model name: `VoidRequest`      → `@@map("void_requests")`
Prisma enum name:  `VoidRequestStatus` → values `pending` | `approved` | `rejected`
Named relations on `User` (must be distinct from the existing `"CreatedBy"` / `"LastEditedBy"`
relations that `Sale` already claims): `"VoidRequestedBy"` and `"VoidReviewedBy"`.

Field list for the model, in this order:
  id, organizationId, organization, saleId, sale, reason, status,
  requestedById, requestedBy, requestedByUsername,
  reviewedById, reviewedBy, reviewedByUsername, reviewedAt,
  createdAt, updatedAt

Column types: `reason String @db.Text` (same as `Sale.notes`);
`requestedByUsername String @db.VarChar(100)` and `reviewedByUsername String? @db.VarChar(100)`
(same as `Sale.createdByUsername` / `Sale.lastEditedByUsername`);
`status VoidRequestStatus @default(pending)`.

Indexes: `@@index([organizationId, status])`, `@@index([organizationId, saleId])`,
`@@index([organizationId, createdAt])`.

Undeclared DB-only guard (NOT in schema.prisma — precedent: the `shifts.openLock` column and
the unmanaged `sessions` table, both documented in comments in schema.prisma):
  column `pendingLock` INT GENERATED ALWAYS AS (IF(`status` = 'pending', `saleId`, NULL)) STORED
  UNIQUE INDEX `void_requests_organizationId_pendingLock_key` (`organizationId`, `pendingLock`)

Shared type file to create: `packages/shared/src/types/voidRequest.ts`
Barrel to extend:            `packages/shared/src/types/index.ts` (uses `.js` extensions in
ESM import paths — see the header comment in that file)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VoidRequestStatus enum, VoidRequest model, and back-relations to schema.prisma</name>
  <files>packages/backend/prisma/schema.prisma</files>
  <read_first>
    packages/backend/prisma/schema.prisma — read the entire file (235 lines) before editing. Note: the enum block (lines 13-29) groups all enums together above the models; the `Sale` model (lines 152-190) is the field-ordering, snapshot-column and index-naming precedent; the `AuditLog` model (lines 192-214) is the precedent for a transition/append table; the `Shift` model comment (lines 141-146) is the precedent for documenting a DB-only generated column that is deliberately absent from the schema; the closing comment (lines 232-234) documents the unmanaged `sessions` table.
    .planning/phases/12-moderator-void-requests/12-PATTERNS.md — the `schema.prisma (model)` section lists the exact conventions to copy (named relations, snapshot FK pattern, `@@map` snake_case, index shape).
  </read_first>
  <action>
Edit `packages/backend/prisma/schema.prisma` as follows.

1. In the Enums block, immediately after the existing `AuditAction` enum (which ends at line 29) and before the `// ─── Models ───` divider, add a `VoidRequestStatus` enum with three lowercase values in this order: `pending`, `approved`, `rejected`. Match the surrounding enum formatting exactly (PascalCase enum name, bare lowercase values, no `@map`).

2. Add back-relations to three existing models. Do not reorder or alter any other field:
   - `Organization` (lines 33-49): add `voidRequests VoidRequest[]` to the relation list, after the existing `shifts Shift[]` line.
   - `User` (lines 51-73): add two named back-relations after the existing `shifts Shift[]` line — `voidRequestsMade VoidRequest[] @relation("VoidRequestedBy")` and `voidRequestsReviewed VoidRequest[] @relation("VoidReviewedBy")`. Distinct relation names are mandatory: `User` already owns `"CreatedBy"` and `"LastEditedBy"` toward `Sale`, and two unnamed relations between the same pair of models is a Prisma validation error.
   - `Sale` (lines 152-190): add `voidRequests VoidRequest[]` after the existing `auditLogs AuditLog[]` line.

3. Add a new `VoidRequest` model after the `AuditLog` model and before `InviteToken`, using the field list, column types and indexes given in the `<interfaces>` block above. Specifically:
   - `id Int @id @default(autoincrement())`
   - `organizationId Int` + `organization Organization @relation(fields: [organizationId], references: [id])` (CLAUDE.md Rule 5)
   - `saleId Int` + `sale Sale @relation(fields: [saleId], references: [id])` — non-nullable, mirroring how `AuditLog.saleId` links back to a sale but required here since a request without a sale is meaningless
   - `reason String @db.Text`
   - `status VoidRequestStatus @default(pending)`
   - `requestedById Int` + `requestedBy User @relation("VoidRequestedBy", fields: [requestedById], references: [id])` + `requestedByUsername String @db.VarChar(100)`
   - `reviewedById Int?` + `reviewedBy User? @relation("VoidReviewedBy", fields: [reviewedById], references: [id])` + `reviewedByUsername String? @db.VarChar(100)` + `reviewedAt DateTime?`
   - `createdAt DateTime @default(now())` + `updatedAt DateTime @updatedAt`
   - the three `@@index` entries and `@@map("void_requests")`

4. Directly above the `@@index` lines inside the `VoidRequest` model, add a block comment — written in your own words, not copied verbatim from this plan — recording that the live table additionally carries a STORED generated column named `pendingLock` plus a unique index over `(organizationId, pendingLock)`, that this column is created by the Task 2 migration and is intentionally not declared in this schema (same precedent as the `Shift` model's note about its own generated column and the unmanaged `sessions` table), that Prisma never reads or writes it, and that it exists purely to make CONTEXT.md D-02 race-safe at the database layer.

Do NOT add a `deletedAt` field or any soft-delete flag to `VoidRequest` — a Void Request is a permanent review record and is never removed (CLAUDE.md Rule 3 applies as "never delete", and there is no hide-from-list surface for it). Do NOT modify the `SaleStatus` or `AuditAction` enums — Approve reuses the existing `void` AuditAction value (CONTEXT.md canonical_refs). Do NOT touch `Product`, `Mop`, `Receiver`, `Shift`, `AuditLog` or `InviteToken` field definitions.
  </action>
  <verify>
    <automated>cd "D:/project/custom projects/alejinput/packages/backend" && npx prisma validate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` (run from `packages/backend`) exits 0 with no relation-ambiguity error
    - `grep -c "^enum VoidRequestStatus" packages/backend/prisma/schema.prisma` returns 1
    - `grep -c "^model VoidRequest" packages/backend/prisma/schema.prisma` returns 1
    - `grep -c '@@map("void_requests")' packages/backend/prisma/schema.prisma` returns 1
    - `grep -c 'VoidRequestedBy' packages/backend/prisma/schema.prisma` returns 2 (the `User` back-relation and the `VoidRequest` forward relation)
    - `grep -c 'VoidReviewedBy' packages/backend/prisma/schema.prisma` returns 2
    - `grep -n 'reason         String' packages/backend/prisma/schema.prisma` shows the field annotated `@db.Text`
    - `grep -c '@@index(\[organizationId, status\])' packages/backend/prisma/schema.prisma` returns 2 (the pre-existing one on `Sale` plus the new one on `VoidRequest`)
    - `grep -c 'enum SaleStatus' packages/backend/prisma/schema.prisma` returns 1 and `grep -c 'enum AuditAction' packages/backend/prisma/schema.prisma` returns 1 — both enums still present and unchanged in value set
    - The `VoidRequest` model block declares no soft-delete timestamp field and no active-flag field (source assertion over field declarations; an explanatory comment mentioning the concept is fine)
  </acceptance_criteria>
  <done>`prisma validate` passes. `VoidRequestStatus` enum and `VoidRequest` model exist with organizationId, saleId, reason (TEXT), status, requester/reviewer snapshot fields and three composite indexes; Organization, User and Sale carry the matching back-relations under distinct relation names; no existing model's fields were altered.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Create and apply the void_requests migration manually, then regenerate the Prisma client</name>
  <files>packages/backend/prisma/migrations/&lt;generated&gt;/migration.sql</files>
  <precondition>Docker Desktop is running and the `alejinput-mysql-1` MySQL container is up and healthy, and the gitignored `packages/backend/.env` and root `.env` files are present in this working directory (documented in 09-01-SUMMARY.md). Halt and report if `npx prisma db execute` returns P1001.</precondition>
  <reversibility rating="costly">Applying a DDL migration to the live database is not a one-command undo — reversing it means writing and applying a DROP TABLE migration. It is additive-only (a brand-new table, no existing table altered), so no existing data can be lost, which keeps it `costly` rather than `one-way`.</reversibility>
  <read_first>
    packages/backend/prisma/migrations/20260717173220_add-shift-clock-in-out/migration.sql — read in full. This is the exact precedent for this plan's SQL: a new-table CREATE, a STORED generated column added BEFORE the FK constraints (with the MySQL 8.4 rebuild-algorithm explanation), a unique index over the generated column, and FK constraints where a base column read by an indexed generated column must use `ON UPDATE RESTRICT` instead of this project's usual `ON UPDATE CASCADE`.
    packages/backend/prisma/migrations/20260721081035_add-deleted-at-soft-delete/migration.sql — the shorter Phase 9 precedent for migration header-comment style.
    .planning/phases/09-add-the-option-to-delete-mops-products-and-users-for-the-adm/09-01-SUMMARY.md — the manual migration workflow to replicate verbatim (create the directory and SQL file by hand, apply with `prisma db execute`, register with `prisma migrate resolve --applied`, then `prisma generate`), including its documented failure modes.
    packages/backend/prisma/schema.prisma — the `VoidRequest` model written in Task 1; the SQL below must match its column set and index names exactly.
    packages/backend/prisma.config.ts — confirms the schema and migrations paths are resolved from CWD `packages/backend`.
  </read_first>
  <action>
STATE.md locks the manual migration workflow for this project: the unmanaged `sessions` table (created by express-mysql-session, deliberately absent from schema.prisma) causes `prisma migrate dev` to detect drift and offer a destructive reset. Do NOT run `prisma migrate dev` in any form, including `--create-only`, and do NOT run `prisma db push`. Follow these five steps.

STEP 1 — Create the migration directory by hand:
```bash
cd "D:/project/custom projects/alejinput/packages/backend"
TS=$(date -u +%Y%m%d%H%M%S)
mkdir -p "prisma/migrations/${TS}_add-void-requests"
```
Record `${TS}` — it is reused in Steps 3 and 4.

STEP 2 — Write `prisma/migrations/${TS}_add-void-requests/migration.sql`. Open with a header comment (your own wording) noting: this is the Phase 12 moderator Void Request workflow table; it is additive-only and touches no existing table; `pendingLock` is a DB-level race guard for CONTEXT.md D-02 that mirrors the Phase 7 `shifts.openLock` precedent and is intentionally absent from schema.prisma. Then emit the statements in exactly this order:

  a. `CREATE TABLE \`void_requests\`` with columns:
     `id` INTEGER NOT NULL AUTO_INCREMENT,
     `organizationId` INTEGER NOT NULL,
     `saleId` INTEGER NOT NULL,
     `reason` TEXT NOT NULL,
     `status` ENUM('pending', 'approved', 'rejected') NOT NULL DEFAULT 'pending',
     `requestedById` INTEGER NOT NULL,
     `requestedByUsername` VARCHAR(100) NOT NULL,
     `reviewedById` INTEGER NULL,
     `reviewedByUsername` VARCHAR(100) NULL,
     `reviewedAt` DATETIME(3) NULL,
     `createdAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
     `updatedAt` DATETIME(3) NOT NULL
     plus `INDEX void_requests_organizationId_status_idx(organizationId, status)`,
     `INDEX void_requests_organizationId_saleId_idx(organizationId, saleId)`,
     `INDEX void_requests_organizationId_createdAt_idx(organizationId, createdAt)`,
     `PRIMARY KEY (id)`, and the trailing
     `DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci` clause copied from the Phase 7 `shifts` CREATE.

  b. `ALTER TABLE \`void_requests\` ADD COLUMN \`pendingLock\` INT GENERATED ALWAYS AS (IF(\`status\` = 'pending', \`saleId\`, NULL)) STORED;` — this MUST come before any FK constraint, per the Phase 7 comment about MySQL 8.4 raising a spurious ER_CANNOT_ADD_FOREIGN when a STORED generated column is added to a table that already has FKs.

  c. `ALTER TABLE \`void_requests\` ADD UNIQUE INDEX \`void_requests_organizationId_pendingLock_key\` (\`organizationId\`, \`pendingLock\`);` — MySQL unique indexes permit unlimited NULLs, so only rows currently in `pending` are constrained; approved and rejected rows collapse `pendingLock` to NULL and free the slot (D-03).

  d. Four FK constraints, all `ON DELETE RESTRICT`:
     - `void_requests_organizationId_fkey` → `organizations(id)`, `ON UPDATE CASCADE`
     - `void_requests_saleId_fkey` → `sales(id)`, **`ON UPDATE RESTRICT`** — `saleId` is read inside the indexed generated column expression, and MySQL 8.4 rejects `ON UPDATE CASCADE` on such a base column (identical to the Phase 7 `shifts_userId_fkey` note). Add a comment recording this reason.
     - `void_requests_requestedById_fkey` → `users(id)`, `ON UPDATE CASCADE`
     - `void_requests_reviewedById_fkey` → `users(id)`, `ON UPDATE CASCADE`

STEP 3 — Apply the SQL directly, bypassing drift detection:
```bash
cd "D:/project/custom projects/alejinput/packages/backend"
npx prisma db execute --file "prisma/migrations/${TS}_add-void-requests/migration.sql"
```

STEP 4 — Register it as applied:
```bash
cd "D:/project/custom projects/alejinput/packages/backend"
npx prisma migrate resolve --applied "${TS}_add-void-requests"
```

STEP 5 — Regenerate the client so `prisma.voidRequest` and the `VoidRequestStatus` type exist:
```bash
cd "D:/project/custom projects/alejinput/packages/backend"
npx prisma generate
```

Failure handling:
- `P1001 Can't reach database server` → start Docker Desktop, `docker start alejinput-mysql-1`, wait for the healthcheck, re-run Step 3.
- Missing `DATABASE_URL` / DB env vars → the gitignored `packages/backend/.env` and root `.env` must be present in the working directory; copy them from the main working copy. Never stage or commit them.
- `Cannot add foreign key constraint` on `void_requests_organizationId_fkey` → change only that constraint's clause to `ON UPDATE RESTRICT` (the same MySQL 8.4 generated-column limitation, applied more broadly than expected) and re-run just that statement via `prisma db execute` against a temp file, then continue to Step 4 with the same migration name.
- A partially-applied migration (table created, later statement failed) → do NOT drop and recreate the table; hand-apply the remaining statements one at a time via `prisma db execute` against a temp file containing just that statement, then proceed to Step 4.
  </action>
  <verify>
    <automated>cd "D:/project/custom projects/alejinput/packages/backend" && npx prisma migrate status 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma migrate status` reports the database schema is up to date with no pending migrations and no drift error
    - A directory matching `packages/backend/prisma/migrations/*_add-void-requests/` exists and contains `migration.sql`
    - That SQL file contains `CREATE TABLE \`void_requests\``
    - That SQL file contains `GENERATED ALWAYS AS` and `void_requests_organizationId_pendingLock_key`
    - In that SQL file the `ADD COLUMN \`pendingLock\`` statement appears at a lower line number than the first `ADD CONSTRAINT` statement (generated column before FKs — MySQL 8.4 ordering requirement)
    - That SQL file contains four `ADD CONSTRAINT` statements and the `void_requests_saleId_fkey` one specifies `ON UPDATE RESTRICT`
    - Running `grep -nE 'ALTER TABLE `(sales|audit_log|shifts|products|mops|users|receivers|organizations)`' <that migration.sql>` produces no output — the migration is additive-only
    - Live-database round trip: `npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM void_requests;"` succeeds (table exists and is queryable), and `npx prisma db execute --stdin <<< "SHOW INDEX FROM void_requests WHERE Key_name='void_requests_organizationId_pendingLock_key';"` succeeds
    - `packages/backend/src/generated/prisma/` was regenerated and exposes a `voidRequest` model delegate — confirm with `grep -rl "voidRequest" packages/backend/src/generated/prisma/ | head -1` returning a path
  </acceptance_criteria>
  <done>The `void_requests` table exists in the live MySQL database with all listed columns, three composite indexes, the `pendingLock` STORED generated column, the `(organizationId, pendingLock)` unique index, and four FK constraints. The migration is registered as applied. The Prisma client is regenerated and `prisma.voidRequest` is available to Plan 12-02. No existing table was altered.</done>
</task>

<task type="auto">
  <name>Task 3: Add VoidRequest shared types and export them from the barrel</name>
  <files>packages/shared/src/types/voidRequest.ts, packages/shared/src/types/index.ts</files>
  <read_first>
    packages/shared/src/types/shift.ts — the exact file-shape precedent: a leading comment naming the phase and entity, then the interface with inline `// ISO 8601 UTC string` annotations on date fields, then an `extends` variant for the enriched response shape (`ShiftWithTotals`).
    packages/shared/src/types/sale.ts — the `SaleStatus` string-union precedent (`export type SaleStatus = 'active' | 'void';`) and the `Sale` interface that `VoidRequestWithSale` embeds.
    packages/shared/src/types/index.ts — the barrel; note the header comment explaining that `.js` extensions are mandatory in ESM import paths even for `.ts` sources under `moduleResolution: node16`.
    packages/backend/prisma/schema.prisma — the `VoidRequest` model written in Task 1; the TypeScript field set must match it one-for-one.
  </read_first>
  <action>
Create `packages/shared/src/types/voidRequest.ts` containing, in this order:

1. A short header comment (your own wording) stating this is the API response shape for the VoidRequest entity introduced by the Phase 12 moderator void-request workflow, and noting that `sale` on the enriched variant is the full snapshot-bearing sale row so the admin table never re-joins products/mops/receivers for display (CLAUDE.md Rule 4).

2. `export type VoidRequestStatus = 'pending' | 'approved' | 'rejected';` — a string union, matching the `SaleStatus` style in `sale.ts`, not an enum.

3. `export interface VoidRequest` with exactly these members and types:
   `id: number`, `organizationId: number`, `saleId: number`, `reason: string`,
   `status: VoidRequestStatus`, `requestedById: number`, `requestedByUsername: string`,
   `reviewedById: number | null`, `reviewedByUsername: string | null`,
   `reviewedAt: string | null`, `createdAt: string`, `updatedAt: string`.
   Annotate `reviewedAt`, `createdAt` and `updatedAt` with an inline ISO 8601 UTC comment, matching the `shift.ts` convention.

4. `export interface VoidRequestWithSale extends VoidRequest { sale: Sale; }` — the shape returned by the admin list and by approve/reject, importing `Sale` via `import type { Sale } from './sale.js';` at the top of the file (note the mandatory `.js` extension).

Then edit `packages/shared/src/types/index.ts`: add one re-export line immediately after the existing `export type { SaleStatus, Sale } from './sale.js';` line, exporting `VoidRequestStatus`, `VoidRequest` and `VoidRequestWithSale` from `'./voidRequest.js'`. Match the surrounding `export type { ... } from '...';` formatting exactly. Do not reorder or remove any existing export line.
  </action>
  <verify>
    <automated>cd "D:/project/custom projects/alejinput" && npm run build --workspace=@alejinput/shared 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/types/voidRequest.ts` exists and `grep -c "export type VoidRequestStatus" packages/shared/src/types/voidRequest.ts` returns 1
    - `grep -c "export interface VoidRequest " packages/shared/src/types/voidRequest.ts` returns 1 and `grep -c "export interface VoidRequestWithSale" packages/shared/src/types/voidRequest.ts` returns 1
    - `grep -n "import type { Sale } from './sale.js'" packages/shared/src/types/voidRequest.ts` matches (the `.js` extension is present)
    - `grep -n "from './voidRequest.js'" packages/shared/src/types/index.ts` matches
    - `grep -c "^export type" packages/shared/src/types/index.ts` returns 9 (the 8 pre-existing barrel lines plus the new one)
    - `npm run build --workspace=@alejinput/shared` exits 0
    - `npm run build --workspace=@alejinput/backend` exits 0 (the regenerated Prisma client from Task 2 plus the new shared types both typecheck)
  </acceptance_criteria>
  <done>`@alejinput/shared` exports `VoidRequestStatus`, `VoidRequest` and `VoidRequestWithSale`; the shared and backend workspaces both build clean; Plans 12-02, 12-03 and 12-04 can import the types from `@alejinput/shared`.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

New symbols — these do not exist before this plan and must be excluded from drift/source-grounding verification:

| Symbol | Kind | Location |
|---|---|---|
| `VoidRequestStatus` (Prisma enum: `pending`/`approved`/`rejected`) | prisma enum | `packages/backend/prisma/schema.prisma` |
| `VoidRequest` | prisma model → table `void_requests` | `packages/backend/prisma/schema.prisma` |
| `Organization.voidRequests` | prisma back-relation | `packages/backend/prisma/schema.prisma` |
| `User.voidRequestsMade` (`"VoidRequestedBy"`) | prisma named back-relation | `packages/backend/prisma/schema.prisma` |
| `User.voidRequestsReviewed` (`"VoidReviewedBy"`) | prisma named back-relation | `packages/backend/prisma/schema.prisma` |
| `Sale.voidRequests` | prisma back-relation | `packages/backend/prisma/schema.prisma` |
| `void_requests` table + `pendingLock` generated column + `void_requests_organizationId_pendingLock_key` unique index | database objects | `packages/backend/prisma/migrations/<generated>/migration.sql` |
| `prisma.voidRequest` delegate | generated client | `packages/backend/src/generated/prisma/` |
| `VoidRequestStatus` (TS string union) | shared type | `packages/shared/src/types/voidRequest.ts` |
| `VoidRequest` (TS interface) | shared type | `packages/shared/src/types/voidRequest.ts` |
| `VoidRequestWithSale` (TS interface) | shared type | `packages/shared/src/types/voidRequest.ts` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application process → MySQL database | This plan introduces a new persisted table. All writes to it arrive through the Prisma client in the backend process; there is no direct client access. The boundary matters here because the DDL decides what the database itself can enforce versus what must be enforced in application code. |
| Migration tooling → live database | `prisma db execute` runs arbitrary DDL against the production-shaped database with no drift check. A wrong statement here is a data-integrity event, not a code bug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | `void_requests` D-02 "one pending per sale" invariant | high | mitigate | Enforced at the database layer by the `pendingLock` STORED generated column plus the `(organizationId, pendingLock)` unique index, not only by an application-level `findFirst` check — two concurrent inserts cannot both win. Plan 12-02 additionally catches the resulting P2002 and maps it to `409 DUPLICATE_PENDING_REQUEST`. |
| T-12-02 | Elevation of Privilege | Cross-tenant reads/writes on `void_requests` | high | mitigate | `organizationId` is NOT NULL with a FK to `organizations` and is the leading column of every index including the uniqueness guard, so every query in Plan 12-02 is forced to scope by organization (CLAUDE.md Rule 5). No query path exists that can omit it and still use an index. |
| T-12-03 | Denial of Service | Unbounded `reason` TEXT column | low | accept | `reason` is `TEXT` like the existing `sales.notes` column, so it inherits the same exposure the app has already accepted for eight phases. Plan 12-02 adds an `express-validator` length bound on the create endpoint as the practical cap; no DDL-level limit is imposed. |
| T-12-04 | Repudiation | Deletion of a review record | medium | mitigate | No `deletedAt`/`isActive` column exists on `VoidRequest` and no delete endpoint is planned in any Phase 12 plan — the record set is append-and-transition only, so a review decision cannot be erased (CLAUDE.md Rule 3). |
| T-12-05 | Tampering | Destructive migration against the live database | high | mitigate | The manual `db execute` + `migrate resolve` workflow is mandated (never `migrate dev`, never `db push`), the SQL is additive-only, and an acceptance criterion greps the migration for any `ALTER TABLE` against an existing table and fails if one is found. |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it installs no packages. Every dependency used (`prisma`, `@prisma/client`) is already present in `packages/backend/package.json` from Phase 1. No package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `npx prisma validate` exits 0 from `packages/backend`
- `npx prisma migrate status` reports no pending migrations and no drift
- The live database answers `SELECT COUNT(*) FROM void_requests;` and `SHOW INDEX FROM void_requests WHERE Key_name='void_requests_organizationId_pendingLock_key';`
- `npm run build --workspace=@alejinput/shared` and `npm run build --workspace=@alejinput/backend` both exit 0
- Manual spot-check (informational): insert two rows with the same `saleId` and `status='pending'` via `prisma db execute` and confirm the second is rejected with a duplicate-key error; then update the first to `status='rejected'` and confirm a new pending row for that same `saleId` now inserts successfully (D-03)
</verification>

<success_criteria>
- `VoidRequestStatus` enum and `VoidRequest` model exist in `schema.prisma` with distinct named relations to `User`, and `prisma validate` passes
- The `void_requests` table exists in the live database with the `pendingLock` generated column and its unique index; migration registered as applied
- No existing table was altered by the migration
- `@alejinput/shared` exports `VoidRequestStatus`, `VoidRequest`, `VoidRequestWithSale`
- Shared and backend workspaces build clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-moderator-void-requests/12-01-SUMMARY.md`
</output>
